[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
monitor_port = /dev/tty.usbserial-*

; Built-in Bluedroid BLE stack — no external library needed.
build_flags =
    -DCORE_DEBUG_LEVEL=0
